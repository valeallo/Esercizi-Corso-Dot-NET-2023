using Arrays.classes.Default;

namespace Arrays.classes
{
    internal class Country : GeographicalArea
    {
        State _state;

        public Country()
        {
            // new State(this);
        }

    }
}
